<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    {{>head}}
</head>
<body>
{{!-- Dashboard Services Page --}}
<div class="container-xxl py-5">
    <div class="container py-5 px-lg-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1 class="mb-0">Manage Services</h1>
            <a href="/dashboard/services/add" class="btn btn-success">Add New Service <i class="fa fa-plus ms-2"></i></a>
        </div>
        <table class="table table-bordered table-hover">
            <thead class="table-dark">
                <tr>
                    <th>#</th>
                    <th>Service Name</th>
                    <th>Category</th>
                    <th>Price</th>
                    <th>Duration</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {{#each services}}
                <tr>
                    <td>{{@index}}</td>
                    <td>{{this.name}}</td>
                    <td>{{this.category}}</td>
                    <td>${{this.price}}</td>
                    <td>{{this.duration}}</td>
                    <td>
                        <a href="/dashboard/services/edit/{{this.slug}}" class="btn btn-warning btn-sm">
                            <i class="fa fa-edit"></i> Edit
                        </a>
                        <form action="/dashboard/services/delete/{{this.slug}}" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure?')">
                                <i class="fa fa-trash"></i> Delete
                            </button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>

{{>footer}}
</body>
</html>